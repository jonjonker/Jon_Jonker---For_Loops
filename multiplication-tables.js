let multiplier = 0

for (let num= multiplier; multiplier <=10; multiplier++)
    for (let num = 1; num <=10; num++) {
        total = num*multiplier;
        console.log(num + " x " + multiplier + " = " + total);
    }
